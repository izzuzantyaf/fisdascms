export enum AdminRole {
  OWNER = 'owner',
  ADMIN = 'admin',
}

export enum Faculty {
  FTE = 'fte',
  FRI = 'fri',
}

export enum Language {
  ID = 'id',
  EN = 'en',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
}

export enum AssistantLevel {
  JUNIOR = 'junior',
  SENIOR = 'senior',
}
